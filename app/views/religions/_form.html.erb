<%= simple_form_for @religion,  wrapper: :inline_form,
  html: { class: 'form-inline' } do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, placeholder: 'Enter new Religion' %>
  <%= f.button :submit %>
<% end %>
