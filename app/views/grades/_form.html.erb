<%= simple_form_for @grade, wrapper: :inline_form,
  html: { class: 'form-inline' } do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, placeholder: 'Enter new grade' %>
  <%= f.button :submit %>
<% end %>
