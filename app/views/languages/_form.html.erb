<%= simple_form_for @language,  wrapper: :inline_form,
  html: { class: 'form-inline' } do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, placeholder: 'Enter new Language' %>
  <%= f.button :submit %>
<% end %>
