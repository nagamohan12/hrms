<%= simple_form_for @location,  wrapper: :inline_form,
  html: { class: 'form-inline' } do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, placeholder: 'Enter new location' %>
  <%= f.input :is_active %>
  <%= f.button :submit %>
<% end %>
