<%= simple_form_for @designation,  wrapper: :inline_form,
  html: { class: 'form-inline' } do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, placeholder: 'Enter new designation' %>
  <%= f.input :is_active %>
  <%= f.button :submit %>
<% end %>
